[package]
name = "rasen-packer"
version = "0.5.1"
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "CLI packer and inspector for RPAK archives"

[dependencies]
clap = { version = "4.5", features = ["derive", "env"] }
rasen-archive = { path = "../rasen-archive" }
